<template name="button">
    <view style="width: 98%;padding-top: 20rpx">
        <button disabled="{{book_name == '' ? true : false}}" style="color: white; background-color: {{bcolor}}; border: none; font-size: 32rpx; line-height: 80rpx; height: 80rpx;" bindtap="{{bindtapaction}}" loading="{{loading}}">{{ context }}</button>
    </view>
</template>